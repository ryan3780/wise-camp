1. VScode에서 터미널을 연다 => 단축키 ( ctrl + ` )

2. data 폴더에 JSON으로 변환할 엑셀 파일들을 넣는다.

3. 터미널에 명령어를 입력하고, 엔터치지 말고, data폴더를 드래그 해서 터미널에 드랍한다.

예) node .\index.js c:\Users\Soma\Desktop\엑셀\data

4. 엔터를 친다.

5. data 폴더에 변환할 엑셀 파일의 이름으로 폴더들이 생성되어 있다.

6. 디스크 스테이션에 올린다.
